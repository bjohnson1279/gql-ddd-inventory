export enum IntegrationPlatform {
  Shopify = 'shopify',
}

export enum ExternalEntityType {
  Product = 'product',
  Variant = 'variant',
  Location = 'location',
  InventoryItem = 'inventory_item',
}
